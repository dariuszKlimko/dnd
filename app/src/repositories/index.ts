import { FilmRepository } from "@app/repositories/film.repository";
import { SpeciesRepository } from "@app/repositories/species.repository";
import { VehicleRepository } from "@app/repositories/vehicle.repository";

export default [FilmRepository, SpeciesRepository, VehicleRepository];
